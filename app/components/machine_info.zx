const Labels = View.Labels;
const PlatformInfo = Result.PlatformInfo;

pub fn MachineInfo(allocator: zx.Allocator, props: struct {
    labels: Labels,
    platform: PlatformInfo,
}) zx.Component {
    const os_arch = std.fmt.allocPrint(allocator, "{s} / {s}", .{ props.platform.os, props.platform.arch }) catch unreachable;

    return (
        <div @allocator={allocator} class="machine">
            <div class="info-tip-wrap machine-wrap">
                <div class="machine-trigger">
                    <div class="machine-line">
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                        </svg>
                        <span>{props.labels.platform}{" • "}{props.labels.tool}{" • "}{props.labels.mode}</span>
                    </div>
                </div>
                <div class="info-tip">
                    <div class="info-tip-arrow"></div>
                    <div class="info-tip-title">Benchmark Environment</div>

                    <div class="info-tip-section">
                        <div class="info-tip-section-label">Load Generator</div>
                        <div class="info-tip-grid">
                            <div class="info-tip-kv"><dt>Tool:</dt><dd>{props.labels.tool}</dd></div>
                            <div class="info-tip-kv"><dt>Mode:</dt><dd>{props.labels.mode}</dd></div>
                            <div class="info-tip-kv"><dt>Threads:</dt><dd>{props.labels.threads}</dd></div>
                            <div class="info-tip-kv"><dt>Connections:</dt><dd>{props.labels.connections}</dd></div>
                            <div class="info-tip-kv"><dt>Duration:</dt><dd>{props.labels.duration}</dd></div>
                            <div class="info-tip-kv"><dt>Target rate:</dt><dd>{props.labels.rate}</dd></div>
                            <div class="info-tip-kv"><dt>Runs:</dt><dd>{props.labels.runs}</dd></div>
                        </div>
                    </div>

                    <div class="info-tip-section">
                        <div class="info-tip-section-label">Primary Platform</div>
                        <div class="info-tip-kv"><dt>Label:</dt><dd>{props.labels.platform}</dd></div>
                        <div class="info-tip-kv"><dt>OS:</dt><dd>{os_arch}</dd></div>
                        <div class="info-tip-kv"><dt>Kernel:</dt><dd>{props.platform.os_version}</dd></div>
                        <div class="info-tip-kv"><dt>Host:</dt><dd>{props.platform.hostname}</dd></div>
                    </div>
                </div>
            </div>
        </div>
    );
}

const std = @import("std");
const zx = @import("zx");
const View = @import("../View.zig");
const Result = @import("../Result.zig");
